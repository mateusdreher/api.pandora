import { SelectNumberUseCase } from './SelectNumberUseCase';
import { Request, Response } from 'express';

export class SeeChosenNumbersController {

    constructor (selectNumberUseCase: SelectNumberUseCase) {

    }
}